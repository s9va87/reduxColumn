const initialState = [
    {id: 1, title: 'One'},
    {id: 2, title: 'Two'},
    {id: 3, title: 'One'},
]

const Todo = (state=initialState, action)=>{
    switch(action.type){
        default:
            return state
    }
}
export  default  Todo;